{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -E '\\.py$' | { read -r f; [ -n \"$f\" ] && cd /home/user/TubeNews && python -m pytest tests/ -q 2>&1 | tail -10; } || true",
            "timeout": 60,
            "statusMessage": "Running pytest..."
          }
        ]
      }
    ]
  }
}
