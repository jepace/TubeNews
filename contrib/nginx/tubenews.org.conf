# nginx reverse-proxy config for tubenews.org
#
# Install on the HOST (not inside the jail):
#   cp tubenews.org.conf /usr/local/etc/nginx/conf.d/tubenews.org.conf
#   service nginx reload
#
# Then run certbot to add TLS:
#   certbot --nginx -d tubenews.org -d www.tubenews.org
#
# TubeNews (gunicorn) runs inside the Bastille jail at 10.0.0.1:8000.
# Flask handles all /content/ serving — nginx proxies everything.

# www → apex redirect
server {
    listen 80;
    listen [::]:80;
    server_name www.tubenews.org;
    return 301 http://tubenews.org$request_uri;
}

# Main site
server {
    listen 80;
    listen [::]:80;
    server_name tubenews.org;

    location / {
        proxy_pass         http://10.0.0.1:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        # Disable buffering so the admin run-log page streams in real time
        proxy_buffering    off;
        proxy_read_timeout 120s;
    }
}
